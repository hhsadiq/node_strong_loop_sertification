docs [http://socket.io/docs/] (http://socket.io/docs/)

```
   methods:
   on
   emit

   for one (socket.emit)
   for other (socket.broadcast)
   for all (io.sockets)

   events:
     connection
     disconnected
```