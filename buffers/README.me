docs [https://nodejs.org/api/buffer.html] (https://nodejs.org/api/buffer.html)

```
Buffer
    new Buffer(size)
    new Buffer(array)
    new Buffer(buffer)

    Buffer.isEncoding
    Buffer.isBuffer
    [obj].length
    [obj].toString(['encoding'])
    [obj].toJSON
    [obj].copy
    [obj].fill

```